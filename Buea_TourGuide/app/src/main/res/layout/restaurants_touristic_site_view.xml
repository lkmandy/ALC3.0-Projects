<?xml version="1.0" encoding="utf-8"?>

<!--card view-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/parent_card_view"
    style="@style/main_activity_layout">

    <!-- A CardView that contains a TextView -->
    <android.support.v7.widget.CardView
        android:id="@+id/card_view"
        style="@style/card_view">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/image_card"
                style="@style/other_images"
                android:src="@drawable/iya" />

            <!--Name-->
            <TextView
                android:id="@+id/name_card"
                style="@style/name_text_view"
                android:text="@string/beta_nights" />

            <!--Hours-->
            <TextView
                android:id="@+id/hours"
                android:text="@string/half_day"
                style="@style/level_two_text_view" />

            <!--Ratings-->
            <TextView
                android:id="@+id/ratings"
                style="@style/level_three_text_view"
                android:text="@string/five" />

        </LinearLayout>

    </android.support.v7.widget.CardView>

</LinearLayout>